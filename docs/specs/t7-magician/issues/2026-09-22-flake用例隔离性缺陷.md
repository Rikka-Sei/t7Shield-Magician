# test_show_step_clears_stale_outcome_classes 用例隔离性缺陷（flake） — Issue

**状态:** open
**发现日期:** 2026-09-22
**来源:** T2 实现者报告 + T9 实现者定向复现
**关联章节:** §10（验证）
**关联决策 ID:** 无（测试基建）

## 1. 现象

`test_show_step_clears_stale_outcome_classes` 非稳定失败（约 1/20 概率；T9 定向复现：隔离冷态 8/8 必失败、热身后必绿）。

## 2. 证据链

| 证据 | 位置 |
|---|---|
| 首次发现（干净基线复现） | T2 实现者报告 `.superpowers/sdd/…/task-2-report.md` |
| 定向复现（冷态 8/8 失败） | T9 实现者报告 `.superpowers/sdd/…/task-9-report.md` |
| 测试本体 | `crates/magi-app/src/ui/main_window.rs` tests 模块 |

## 3. 影响

- `cargo test -p magi-app` 偶发 49/1，干扰所有任务的「全绿」验收口径；
- 冷态必绿/热身后必失败的模式暗示用例间状态泄漏（GTK 全局态或 test_support 的 gtk_ready 单例初始化窗口顺序依赖）。

## 4. 拟议方向

- 用 test_support 的 gtk_ready 机制分析该测试对前序用例的隐式依赖；
- 若是 GTK 全局态（如 AdwStyleManager / css class 残留），在用例开头显式重置或拆出为独立 `#[test]` 进程。

## 5. 状态

open——非阻塞（复跑即绿），但应在下一批测试基建改进中修复。

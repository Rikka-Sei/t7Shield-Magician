# history/

本目录只放日期戳冻结快照（如 `YYYY-MM-DD-<标题>.md`），用于历史对照。

规则：

1. 快照一旦提交即冻结，**不再修改**；后续变更一律写进 `spec.md`，不要回来打补丁。
2. 快照上禁止出现“（已被 Dxx 取代）”之类的修订标注；新旧差异由 git diff 承担。
3. 需要回溯某次行为变更时，查 git 历史 + `spec.md` 决策日志，而不是改快照。

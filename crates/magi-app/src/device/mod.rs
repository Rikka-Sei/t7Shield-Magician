//! 设备域模块（D34）：按单一关注点分模块收进域目录。
//!
//! 除 [`jobs`]（工作线程与事件投递）外均为纯逻辑，无 GTK 依赖（K6：无头可测）；
//! K3 同纪律：只读世界可读文件，不提权；判定逻辑以「根目录注入」保持平台无关可测。

/// §4.13 环境就绪引导（D30）：自检、模块装载/权限修复状态机与固定命令（纯逻辑）。
pub mod environment;

/// §4.12/§6 入口启用规则与口令输入校验（纯逻辑）。
pub mod gate;

/// §4.1/§4.13 作业设施：单飞注册表、工作线程与 `AppEvent` 投递（`spawn_device_job`）。
pub mod jobs;

/// 重枚举观察采样 → `ReEnumerationObservation` 的纯映射与轮询驱动。
pub mod observation;

/// §4.1/§3.3 设备身份准入与周期重扫的呈现裁决（纯逻辑）。
pub mod rescan;

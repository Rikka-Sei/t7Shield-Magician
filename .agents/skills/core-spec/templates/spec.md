<!--
core-spec 模板使用说明：
1. 复制本文件为 docs/specs/<spec-name>/spec.md。
2. 按下面章节顺序填写；[方括号] 内容全部替换，删除本说明注释。
3. 必需章节标题（## 1. 到 ## 10.）不得改名/合并，audit_spec.py 按标题检查。
4. 正文写完前保持 **状态: Draft**；测试锚点真实存在且 barriers 全绿后改为 Authoritative。
5. 规范性条款不得出现“可能/大概/尽量/友好/快速”等模糊词；每条需求必须有 REQ ID。
6. 术语采用 spec 编程标准说法：权威规格、单一事实源、规格契约、验收屏障。
-->

# [系统/能力名] — 权威规格（Spec）

**状态:** Draft（未接线） / Authoritative（唯一权威）
**版本:** 0.1（决策基线：D01–Dxx）
**受众:** [开发 / 测试 / 运维 / 评审；写清谁读哪部分]
**范围:** [一句话界定本 spec 管什么]
**治理:** 行为变更必须先在 §9 决策日志新增或归因决策 ID，同步 `tools/audit_manifest.json`，运行 `python tools/test_audit_spec.py`、`python tools/audit_spec.py`、`python tools/barriers.py` 全部 PASS 后，再进入 plan/代码；被取代条款原地合并或删除，不留补丁标记；spec 与代码同批提交。
**变更历史:** 见 `history/`、`issues/` 与 §9 决策日志。

---

## 1. 背景与目标

### 1.1 为什么写

- 现状问题：[用证据和数字描述，不写形容词]
- 影响：[哪些调用方/路径受影响，具体后果]

### 1.2 目标

- GOAL-1 [可验证目标，验收标准在 §8 引用]
- GOAL-2 [可验证目标]

### 1.3 非目标（Out of Scope）

- 本次不做 [明确列出，防止范围蔓延]

## 2. 术语表

| 术语 | 定义 | 禁止同义词 |
|---|---|---|
| [术语] | [唯一权威定义] | [不得使用的别名] |

## 3. 系统模型与状态机

### 3.1 架构与边界

[架构图（mermaid）+ 依赖方向文字；本 spec 不负责什么]

### 3.2 状态定义

[状态类型定义代码块；每个字段的含义与零值规则]

### 3.3 状态转换

[状态图（mermaid）+ 状态表：进入条件 / 退出条件 / 可观察结果 / 失败分支。每个转换必须写触发条件、guard、action、失败去向]

## 4. 功能需求与接口契约

> **作为** [角色]，**我希望** [动作]，**以便** [价值]。
> **优先级:** P0 / P1 / P2
> **验收标准:**
> Given [前置条件]
> When [动作]
> Then [可观察结果]

- REQ-001 [需求标题]
  [契约代码块：精确签名，一行一句；跨层接口写明“由谁实现、谁消费、何时调用”]
  [示例：正常/异常请求响应，至少各一例]

## 5. 错误模型

| 标识符 | 定义位置 | 产生条件 | 错误链身份（errors.Is/Join） | 消费方 |
|---|---|---|---|---|
| [ErrX] | [包/层] | [精确条件] | [包含/不包含哪个 sentinel] | [谁解释、谁映射] |

## 6. 非功能需求（NFR）

- 性能：[延迟 P95、吞吐、并发，量化]
- 可用性：[恢复窗口、重试上限、退避、降级行为]
- 安全：[认证、授权、敏感数据、日志脱敏、限流]
- 合规与保留：[数据保留周期、清理策略]
- 容量与背压：[队列上限、溢出行为、禁止无界扩容]

## 7. 异常与边界条件

| 场景 | 前置条件 | 系统行为 | 可观察结果 | 测试锚点 |
|---|---|---|---|---|
| [异常/边界] | [条件] | [规定行为] | [可验证结果] | [测试名] |

## 8. 验收标准

- AC-001 [可测量标准，映射 REQ/GOAL；引用测试锚点或验证命令]

## 9. 决策日志

| ID | 已确认决策 | 主要影响章节 | 验证规则 |
|---|---|---|---|
| D01 | [决策内容] | [章节号] | [机器/测试可验证规则] |

## 10. 验证

- 结构审计：`python tools/test_audit_spec.py && python tools/audit_spec.py`
- 验收屏障：`python tools/barriers.py`
- 测试锚点清单：[与 manifest 一致，逐条可 grep]
- 兼容/迁移屏障表：[条件 / 满足标准 / 验收证据]

---

<!-- 项目扩展章节（可选，放在最后，且必须是不改变规范性的验证/记录类内容）：
## 11. 测试矩阵
## 12. 可观测性
## 13. 兼容性矩阵

禁止进入 spec 正文：实施波次（W0–W8 等）、迁移步骤、S0–S5 推进计划、
锁算法与实现级设计推进——这些内容写入 plans/ 或设计文档，spec 只保留
决策归因、契约摘要与指针。
-->
